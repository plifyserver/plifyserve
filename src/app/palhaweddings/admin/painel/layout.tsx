import { PalhaAdminShell } from './PalhaAdminShell'

export default function PalhaAdminPainelLayout({ children }: { children: React.ReactNode }) {
  return <PalhaAdminShell>{children}</PalhaAdminShell>
}
